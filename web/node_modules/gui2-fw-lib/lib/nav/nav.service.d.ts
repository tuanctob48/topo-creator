import { HttpClient } from '@angular/common/http';
import { FnService } from '../util/fn.service';
import { LogService } from '../log.service';
export interface UiView {
    id: string;
    icon: string;
    cat: string;
    label: string;
}
/**
 * ONOS GUI -- Navigation Service
 */
export declare class NavService {
    private _fn_;
    private log;
    private httpClient;
    showNav: boolean;
    uiPlatformViews: UiView[];
    uiNetworkViews: UiView[];
    uiOtherViews: UiView[];
    uiHiddenViews: UiView[];
    constructor(_fn_: FnService, log: LogService, httpClient: HttpClient);
    hideNav(): void;
    toggleNav(): void;
    getUiViews(): void;
}
