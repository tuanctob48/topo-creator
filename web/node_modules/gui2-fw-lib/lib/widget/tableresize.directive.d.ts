import { AfterContentChecked } from '@angular/core';
import { FnService } from '../util/fn.service';
import { LogService } from '../log.service';
import { MastService } from '../mast/mast.service';
/**
 * ONOS GUI -- Widget -- Table Resize Directive
 */
export declare class TableResizeDirective implements AfterContentChecked {
    protected fs: FnService;
    protected log: LogService;
    protected mast: MastService;
    private w;
    pdg: number;
    tables: any;
    constructor(fs: FnService, log: LogService, mast: MastService, w: any);
    ngAfterContentChecked(): void;
    windowSize(tables: any): void;
    onResize(event: any): {
        h: any;
        w: any;
    };
    adjustTable(tables: any, width: number, height: number): void;
    _width(elem: any, width: any): void;
    setHeight(thead: any, body: any, height: number): void;
}
