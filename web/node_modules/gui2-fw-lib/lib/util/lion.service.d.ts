import { LogService } from '../log.service';
import { WebSocketService } from '../remote/websocket.service';
/**
 * A definition of Lion data
 */
export interface Lion {
    locale: any;
    lion: any;
}
/**
 * ONOS GUI -- Lion -- Localization Utilities
 */
export declare class LionService {
    private log;
    private wss;
    ubercache: any[];
    loadCbs: Map<string, () => void>;
    /**
     * Handler for uberlion event from WSS
     */
    uberlion(data: Lion): void;
    constructor(log: LogService, wss: WebSocketService);
    /**
     * Returns a lion bundle (function) for the given bundle ID (string)
     * returns a function that takes a string and returns a string
     */
    bundle(bundleId: string): (string: any) => string;
}
