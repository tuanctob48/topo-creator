import { FnService } from '../util/fn.service';
import { LogService } from '../log.service';
import { ThemeService } from '../util/theme.service';
import { WebSocketService } from '../remote/websocket.service';
export declare class PanelService {
    private funcs;
    private log;
    private ts;
    private wss;
    constructor(funcs: FnService, log: LogService, ts: ThemeService, wss: WebSocketService);
    createPanel(id: any, opts: any): {
        show: (cb: any) => void;
        hide: (cb: any) => void;
        toggle: (cb: any) => boolean;
        empty: () => any;
        append: (what: any) => any;
        width: (w: any) => number;
        height: (h: any) => number;
        bbox: () => any;
        isVisible: () => boolean;
        classed: (cls: any, bool: any) => any;
        el: () => any;
    };
    destroyPanel(id: any): void;
}
