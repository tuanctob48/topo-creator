import { FnService } from '../util/fn.service';
import { LogService } from '../log.service';
import { ThemeService } from '../util/theme.service';
/**
 * ONOS GUI -- Layer -- Loading Service
 *
 * Provides a mechanism to start/stop the loading animation, center screen.
 */
export declare class LoadingService {
    private fs;
    private log;
    private ts;
    private w;
    images: any[];
    idx: number;
    img: any;
    theme: string;
    task: any;
    wait: number;
    constructor(fs: FnService, log: LogService, ts: ThemeService, w: any);
    dbg(...args: any[]): void;
    preloadImages(): void;
    addImg(theme: string, idx: number): void;
    fname(i: number, theme: string): string;
    nextFrame(): void;
    startAnim(): void;
    stopAnim(): void;
    start(): void;
    stop(): void;
    waiting(): boolean;
}
