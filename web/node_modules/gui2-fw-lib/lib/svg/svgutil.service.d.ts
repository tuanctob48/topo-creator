import { FnService } from '../util/fn.service';
import { LogService } from '../log.service';
/**
 * ONOS GUI -- SVG -- Util Service
 *
 * The SVG Util Service provides a miscellany of utility functions.
 */
export declare class SvgUtilService {
    private fs;
    private log;
    constructor(fs: FnService, log: LogService);
    translate(x: number[], y?: any): string;
    scale(x: any, y: any): string;
    skewX(x: any): string;
    rotate(deg: any): string;
    cat7(): {
        testCard: (svg: any) => void;
        getColor: (id: any, muted: any, theme: any) => any;
    };
}
