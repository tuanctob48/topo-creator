import { FnService } from '../util/fn.service';
import { LogService } from '../log.service';
import { SvgUtilService } from './svgutil.service';
/**
 * ONOS GUI -- SVG -- Glyph Service
 */
export declare class GlyphService {
    private fs;
    private log;
    private sus;
    glyphs: any;
    api: Object;
    constructor(fs: FnService, log: LogService, sus: SvgUtilService);
    warn(msg: string): void;
    addToMap(key: any, value: any, vbox: any, overwrite: boolean, dups: any): void;
    reportDups(dups: string[], which: string): boolean;
    reportMissVb(missing: string[], which: string): boolean;
    clear(): void;
    init(): void;
    registerGlyphs(data: Map<string, string>, overwrite?: boolean): boolean;
    registerGlyphSet(data: Map<string, string>, overwrite?: boolean): boolean;
    ids(): any;
    glyph(id: any): any;
    glyphDefined(id: any): any;
    /**
     * Load definitions of a glyph
     *
     * Note: defs should be a D3 selection of a single <defs> element
     */
    loadDefs(defs: any, glyphIds: string[], noClear: boolean): void;
    addGlyph(elem: any, glyphId: string, size: number, overlay: any, trans: any): any;
}
